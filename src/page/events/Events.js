import React from 'react';
import MainLayout from '../../components/MainLayout';

const Events = () => {
    return (
        <MainLayout>
          events  
        </MainLayout>
    );
};

export default Events;